[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'uvicorn api.heavy:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/discovery"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
