[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn api.heavy:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/discovery"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
